/* ==========================================================================
   MuraMap — настройки сайта
   siteUrl — адрес, по которому сайт открыт в интернете (со слешем в конце).
   Из него строятся ссылки внутри QR-кодов, и по нему сканер проверяет,
   что код «наш».

   После публикации на GitHub Pages замените USERNAME на свой логин GitHub,
   а MuraMap — на название репозитория, если оно другое.
   ========================================================================== */

window.MURA_CONFIG = {
  siteUrl: 'https://dkktqqyvpx-ops.github.io/MuraMap/',
   chatUrl: 'https://muramap-guide.nurdaulet-malikov.workers.dev/'
};
